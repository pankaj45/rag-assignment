package com.example.secfix.repository;

import com.example.secfix.domain.GeneratedAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GeneratedAnswerRepository extends JpaRepository<GeneratedAnswer, Long> {
}

